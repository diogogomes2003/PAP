import { ConfirmarPasswordDirective } from './confirmar-password.directive';

describe('ConfirmarPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmarPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
